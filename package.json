{
  "scripts": {
    "check": "npm run typecheck && npm run test:workflow",
    "docs:prepare": "tsx scripts/visual-doc-workflow.ts prepare",
    "docs:validate": "tsx scripts/visual-doc-workflow.ts validate",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:workflow": "vitest run scripts/visual-doc-workflow.test.ts",
    "typecheck": "tsc --noEmit",
    "workflow": "tsx scripts/visual-doc-workflow.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/prompts": "^2.4.9",
    "commander": "^14.0.3",
    "prompts": "^2.4.2",
    "shadcn": "^3.8.5",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "zod": "^4.4.1"
  }
}
